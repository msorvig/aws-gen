[package]
name    = "multi-example"
version = "0.1.0"
edition = "2021"

[dependencies]
aws-runtime-async = { path = "../../aws-runtime-async" }
tokio              = { version = "1", features = ["rt", "macros"] }

[build-dependencies]
aws-codegen = { path = "../../codegen" }
